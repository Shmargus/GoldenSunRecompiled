# GS-011 POSITION-INDEPENDENT AOT image for Func_15570.
#
# Func_15570 is one of the routines the flash driver copies into its relocatable
# working area. Bases observed so far: 0x03002140, 0x030035bc, 0x03003cdc. Routine and base
# vary independently, so that list is a record of what a run happened to
# reach, not a closed set.
#
# Generated with `gba_recompile --relocatable-image 0x030035bc:0x60`: every guest
# address the image derives from its own PC is emitted as
# g_runtime_image_base + offset. 0x030035bc is the generation ORIGIN only - the
# dispatch table, goto labels and resume tokens stay expressed in it, and the
# runtime registry translates a live PC by subtracting the verified base.
#
#
# Generated by tools/build_transient_image_config.py from the pinned
# ELF and the hash-verified ROM. The extent is the ELF STT_FUNC size;
# the resume/data split below is the function's own mapping symbols
# translated through the copy bias, not hand arithmetic.
#
# Evidence: flash-driver routine observed at 0x03002140, 0x030035bc and 0x03003cdc, always contiguous after Func_15430; DMA3 copies 0x18 words from 0x08015570
#
# ELF: Func_15570 at 0x08015570, size 96 (0x60), mode arm.
# Image SHA-1 20f9190c9b7aac90e091beff5f7e54a46ba833eb.

[program]
name = "Golden Sun relocatable Func_15570 RAM image"
id = "golden_sun_usa_transient_func_15570_relocatable"
load_address = 0x08000000
size = 0x00800000
entry_pc = 0x030035bc
speculative_literal_harvest = false
codegen_shards = 1

[identity]
sha1 = "5c4695205413df7db52b9a184815a07783999971"

[[code_copy]]
runtime_start = 0x030035bc
source_start = 0x08015570
size = 0x00000060
name = "relocatable_Func_15570_image"
note = "flash-driver routine observed at 0x03002140, 0x030035bc and 0x03003cdc, always contiguous after Func_15430; DMA3 copies 0x18 words from 0x08015570; image SHA-1 20f9190c9b7aac90e091beff5f7e54a46ba833eb"

[[extra_func]]
addr = 0x030035bc
mode = "arm"
name = "Func_15570_relocatable_image"
note = "ELF STT_FUNC entry Func_15570 installed at the observed copy destination"

[[resume_range]]
start = 0x030035bc
end = 0x03003618
mode = "arm"
note = "Func_15570 code run, ROM 0x08015570..0x080155cc"

[[data_range]]
start = 0x03003618
end = 0x0300361c
note = "ELF $d run inside Func_15570, ROM 0x080155cc..0x080155d0"
