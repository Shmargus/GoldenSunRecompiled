# Build outputs
build/
out/
cmake-build-*/
CMakeFiles/
CMakeCache.txt
*.obj
*.o
*.a
*.lib
*.dll
*.exe
*.pdb

# Generated recompilation output and caches
generated/*
!generated/README.md
recomp_cache/
recomp_coverage*
recomp_master_misses*
recomp_seed_proposals*
*.toml.frag

# ROM, BIOS, saves, states, traces, dumps, extracted assets
*.gba
*.bin
*.rom
*.sav
*.srm
*.ss?
*.state
*.trace
*.dump
*.raw
*.pcm
*.wav
*.mid
*.sf2
*.png
*.bmp
*.tga
*.gif
roms/*
!roms/README.md
private/
traces/
snapshots/
extract/
extracted/

# Local upstream builds/inventories that may contain protected data
local/
upstream-build/
goldensun-disasm-build/
game.toml
bios.cfg
rom.cfg

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# IDE/OS
.vs/
.vscode/settings.json
.idea/
.DS_Store
Thumbs.db
*.user

# Private machine paths for scripts/gs.ps1 (ROM/BIOS/ELF locations).
config/local.json

# Session debug artifacts (local only)
hang_trace.csv
hang_dump.log
delay_sweep.out
delay_sweep.err
localcanon_state5_run.out.log
localcanon_state5_run.err.log

# GoldenSunLauncher per-session stdout/stderr capture (local only, never
# committed — see src/launcher_main.cpp SessionLogWriter).
logs/

# Splash artwork: excluded pending provenance confirmation (see LEGAL.md)
gssplash.jpg

# Stray build logs
lto_build.log
*_build.log

# Runtime debug artifacts from ad-hoc probe scripts (local only)
battle_log_*.txt
battle_probe_*.txt
crash_err.txt
crash_out.txt
run_state.txt

# Ad-hoc disassembly/symbol dumps from debugging sessions (local only,
# regenerable from the built exe with objdump/nm)
dis_*.txt
text_syms*.txt
syms*.txt
err*.txt
e[0-9].txt

# Local convenience scripts that hardcode private ROM/BIOS/savestate paths.
# Kept on disk, never committed — see SECURITY.md.
CATCH BATTLE LOG.ps1
RUN BATTLE PROBE.ps1

# Local trace launcher (see WIDE-05); regenerable, not committed
TRACE MAP.ps1
logs/mmio_map_*.csv

# Local Claude Code settings carry absolute paths; never commit them.
.claude/settings.local.json

# Superseded working notes: handoffs, old status files and closed issues.
# Kept on disk, out of the repo -- the live documentation is README.md,
# AGENTS.md, ARCHITECTURE.md, ROADMAP.md, FACTS.md and docs/.
docs/OLD/
