# Active issue index

Updated 2026-08-13 from `session_20260813_172202` and the post-bandit
checkpoint. Faithful behavior remains the default.

| ID | Issue | Evidence / status | Next gate |
|---|---|---|---|
| PERF-01 | Battle attack/Psynergy stalls | RESOLVED as two causes. Our own compile-storm bug is fixed (`overlay_loader.cpp` identity-window + warm-lookup fix): interpreted_insns 870,336 → 34,221, worst frame 136.8ms storm → 49.8ms one-time hitch. Remaining CPU-bound heaviness is the real GBA's own 1x-speed cost, now addressable by the shipped guest CPU overclock (user-confirmed smooth at 2x/4x). See `docs/CODEX_HANDOFF.md` and `docs/SESSION_2026-08-13B.md`. | Overclock throughput gain is user-observed only, not instrumented — needs a CPU-bound (e.g. mid-summon) savestate to measure with a probe. |
| PERF-03 | Possible missing audio at certain events (unconfirmed) | User suspects audio may be missing at some in-game events, but is not sure whether those moments are intentionally silent in the original game. This is a suspicion, not a confirmed defect — not yet investigated. | Needs the user to name a specific, reproducible event first. Once named: compare against a known-good reference of the original game's behavior at that same event. Nothing to measure until a concrete moment is identified. |
| PERF-02 | Interpolation feels uneven | Only 2,885/9,118 midpoint opportunities succeeded (31.6%); safe duplicate fallback was 68.4%. PPU cost is small compared with PERF-01. Fallback logging also produced 1,998 durable log writes; it is now rate-limited with exact shutdown totals retained. | Measure battle and field coverage/cadence again. World map remains unsupported. |
| PRES-01 | Enhanced Timing appears to remove lag | It fixes display/menu cadence, not CPU stalls. On the measured 120 Hz panel, faithful timing adds an uneven refresh about every 1.8 s; exact 60 Hz reduces that to about every 160 s. The user's choice now persists; first run remains OFF because it deliberately speeds wall cadence 0.456% and the one-hour A/V drift gate remains open. | Keep selected ON for this 120 Hz setup; complete long A/V drift before making it universal default. |
| PRES-02 | Screen “tearing” | D3D11 flip presentation reports VSync active. Current evidence fits cadence judder/shimmer, not a torn swap. | Re-test with Enhanced Timing; capture a photo/video only if a horizontal tear remains. |
| PRES-03 | Ivan/world-map shimmer | Ivan-specific cause is TODO-EVIDENCE. World-map interpolation and native supersampling both fail their exact endpoint gates, so unsafe temporal smoothing stays disabled. Added default-off `Reduce shimmer (soft filtering, experimental)` using ordinary whole-image filtering. | User A/B. Accept softness or turn it off; do not weaken endpoint verification. |
| PRES-04 | Unstable normal-speed FPS | Root cause fixed on the launcher path: pacing happened after the visible present, so variable work created alternating long/short picture gaps. Moving the 1x wait before present cut state-9 gap deviation from 678 us to 16.6 us; p1/p99 tightened from 14.57/18.63 ms to 16.72/16.77 ms, with no gaps over 18 ms. VSync and Enhanced Timing were not the cause. | User visual smoke in cave and battle. Keep 2x/PIP behavior separate. |
| UI-01 | Turbo audio and ceiling | Added persisted mute and 32x slider. Latest config used Uncapped, so the slider was intentionally ignored. VSync still capped the old path near 2x by presenting every guest frame. Turbo now omits intermediate render/VSync work while pumping input/audio and refreshing the display at least every 16.7 ms. Current compute headroom is about 4x, not 32x. | User smoke Turbo again; `[turbo-present]` reports presented/skipped frames. |
| COV-01 | New post-bandit progression coverage | User checkpoint “state 10” is the tenth hotkey slot, stored as `Golden Sun.state9`. Latest route completed but logged new ROM/overlay/RAM misses. | Prove new immutable entries against pinned ELF data, regenerate, and re-run from state9. |
| CRASH-01 | **RESOLVED 2026-08-14, user-confirmed.** Hard abort leaving Vale for the overworld, end of prologue | Reproduced by the user 2026-08-14 on a fresh-boot playthrough; log `logs/session_20260814_072447.log`. Fatal line: `runtime_arm: generated call-return stack overflow at return_pc=0x03002104` — a deliberate `std::abort()` in `runtime_call_push_return` (`gbarecomp/src/armv4t/runtime_arm.cpp:1114-1126`) when `g_call_return_depth >= kCallReturnStackSize`. **This is our bug, not the game's.** The 96-event trace ring proves it: guest `sp` is CONSTANT at `0x03007E94` across every event, so the guest is looping, not recursing — only our shadow return-address stack grows. Repeating shape: push (`aux=0x1`) of return_pc `0x030020F4` or `0x03002104`, then a `runtime_call_should_return` query for `0x0300210C` that finds NO match (`aux=0x3`) — net +1 depth per iteration. Depth observed climbing `0x3FE` → `0x400`, so the ceiling is ~1024. All PCs are IWRAM runtime-copied code (`gf_afunc_030020F4`, `gf_afunc_03002108`, `gf_copied_Func_15570_03002140`, `gf_copied_Func_15430_03002000`) writing single bytes to steadily increasing EWRAM (`0x0200E727`+) — looks like a byte-at-a-time decompression loop, consistent with unpacking overworld map data. Note `0x03002xxx` is a DIFFERENT range from PERF-01's known dynamic-RAM blit code at `0x03006000..0x0300645C`. **ROOT CAUSE PROVEN 2026-08-14.** The live image at `0x03002000` is `transient_Func_2544` (`transient_func2544_pic`), NOT `copied_Func_15430/15570` — proven by instruction-form cross-check against the trace, since the log's symbol labels resolve every PC against the primary table regardless of which candidate `overlay_try_dispatch` selected. That image uses a **shared-trampoline idiom**: three `BL` sites (`0x0300208C`, `0x030020F0`, `0x03002100`) all call `0x03002220`, and that callee computes its return unconditionally as `sub r14,r15,#0x11c` → always `0x0300210C` regardless of caller, then `bx r14`. Legitimate ARM, not a game bug. So `should_return(0x0300210C)` misses, `runtime_dispatch` runs `0x0300210C` as a nested synchronous C call which pushes more frames, and by the time control unwinds to the call site's `if (R15 != pushed) cancel_return(pushed)` check, our frame is buried. `runtime_call_cancel_return` (`runtime_arm.cpp:1149-1157`) pops **only when the frame is exactly on top**, so it silently no-ops and strands one frame per loop iteration → 1024 → abort. **Fix (CRASH-01c, in progress): make `cancel_return` scan down to `g_call_return_floor` and truncate, exactly as `should_return` (:1128-1147) already does.** The top-only check was the asymmetry. No codegen change, no `generated/**` regeneration. **FIX BUILT AND VERIFIED 2026-08-14, awaiting user play-test.** `runtime_call_cancel_return` now scans top-down to `g_call_return_floor` and truncates (aux `4u` top-slot / `6u` deeper, mirroring `should_return`'s `2u`/`5u`). Test-only floor accessors added (`runtime_arm.cpp:1197`, `runtime_arm.h:513`); new `run_call_cancel_return_scan_cases()` in `tests/codegen/test_main.cpp:334`. `kCallReturnStackSize` still 1024 and the overflow `abort()` is still in place as a tripwire — deliberately, so a future leak still trips loudly instead of silently corrupting. Build results: `codegen_tests` 131/131 pass including all four new subcases (first ever execution of that function); `GoldenSunRecomp` links at 231,881,358 bytes @ 09:10; Python 86/86; CTest 27/29 with only the two known pre-existing non-passes (`public_repository_audit`, `heal_gate_tests`); headless boot clean with zero occurrences of the overflow abort. One forced incidental fix: `gbarecomp/tests/codegen/stubs.cpp:149` gained stubs for `g_cost_irq_handler_ns` / `g_cost_irq_handler_calls` / `g_cost_probe_enabled()` because the isolated test target could not link against a pre-existing uncommitted cost probe in `runtime_irq()`. **User play-tested and confirmed 2026-08-14: Vale → overworld now works.** The repro that defined this issue no longer reproduces. | Enlarging the stack is NOT the fix — it only delays the abort. The real question is which guest branch form emits a `push_return` with no matching `should_return` hit. Evidence gathering in progress (CRASH-01 worker): the push/pop emission sites per instruction form (BL/BLX/BX/MOV PC/LDR PC/POP{..,PC}), the role of `g_call_return_floor`, and the full depth curve across the run. Fix almost certainly belongs upstream in `gbarecomp`. |
| CLEANUP-02 | Diagnostic cost probe left in `runtime_irq()` | Noticed during BUILD-01, 2026-08-14. `gbarecomp/src/armv4t/runtime_arm.cpp`'s `runtime_irq()` unconditionally references cost-probe globals (`g_cost_irq_handler_ns`, `g_cost_irq_handler_calls`, `g_cost_probe_enabled()`) defined in `runtime_bus_bridge.cpp`. Its own comment marks it "Diagnostic-only; intended for removal after the report". Pre-existing and uncommitted, not introduced by this session's work. It already cost something real: it broke the isolated `codegen_tests` link, which needed three stub definitions added at `gbarecomp/tests/codegen/stubs.cpp:149` to work around. Same class of leftover as the `g_cost_mem_cycles_total_calls` counter already removed under CLEANUP-01a. | Low priority, but decide deliberately: either remove it (and then drop the three stubs again) or keep it and say so, before any commit. Do not remove while the user is mid-playtest — it touches the IRQ path and would force another full rebuild. |
| TOOL-01 | Trace/log symbolization mislabels PCs that run through overlay-selected candidates | Found while diagnosing CRASH-01, 2026-08-14. The trace ring and log resolve every PC against the **primary** symbol table (`local/gs011/main/symbol_map.cpp`) regardless of which registered candidate `overlay_try_dispatch` (`gbarecomp/src/armv4t/runtime_arm.cpp:1044-1049`) actually selected. When two images are registered at the same IWRAM address — as at `0x03002000` — every crash-ring line is labelled with the *wrong* function name. This directly cost an evidence pass during CRASH-01: a worker disassembled the labelled (inactive) image, correctly found no `BL`, and reported a contradiction that took a second pass to resolve. | Not a game bug and not urgent, but it will mislead every future RAM-code/overlay investigation the same way. Fix: symbolize against the candidate actually dispatched, or at minimum print which candidate is live alongside the label. Deferred until CRASH-01 ships. |
| CRASH-02 | Interpreter bridge does not save/restore call-return depth | Latent defect found while diagnosing CRASH-01, 2026-08-14. `runtime_irq()` saves and restores `g_call_return_floor` around its drive-to-completion loop (`gbarecomp/src/armv4t/runtime_arm.cpp:1549`, restored `:1575`). The Stage-1 self-heal interpreter bridge `runtime_dispatch_miss` (`gbarecomp/src/runtime/runtime_arm_default_aborts.cpp:1151`) has **no analogous protection** — grep confirms it never touches `g_call_return_depth` or `g_call_return_floor`. Separately, the interpreter's own `IrOp::BL` (`gbarecomp/src/armv4t/interpreter.cpp:616-621`) and `IrOp::BX` (:637-645) set registers directly and never call `push_return`/`should_return`, so the interpreter tier is asymmetric with the AOT tier by design. A static→interpreter→static crossing could therefore leak or corrupt frames. | **Confirmed NOT the cause of CRASH-01** — no bridge entry occurs anywhere near that crash window (last interpreter bridging is at boot). Real but unproven-in-practice; needs a repro before it is worth touching. Re-check after CRASH-01c ships, since the cancel_return scan fix may mask or interact with it. |
| WIDE-02 | Camera zoom-out slider (%) instead of widescreen | User request 2026-08-14, offered explicitly as an alternative to WIDE-01. Player-chosen percentage slider that zooms the camera out so more of the current map is visible at once, scaled down to fit the normal 240x160 frame — the aspect ratio and the frame itself stay stock, only the scale changes. User's framing: "camera can not show more than base game, but can render more of the current map at once." | **The honest caveat, which WIDE-01 already established: zooming out needs the same off-screen picture data widescreen needed, so it may hit the same wall.** But it is NOT obviously the same answer, and the difference is worth measuring. A regular tiled BG map is 256 or 512 px wide against a 240 px window (`gba_ppu.cpp:1086-1088`), so a modest zoom — up to roughly 2x on a 512 px map — could be sampling REAL tile data rather than the wrapped seam. The open question is whether Golden Sun keeps the full map ring populated or streams tiles in just ahead of the visible window, leaving stale/garbage tiles in the off-window part of the ring. Determine that before building anything. Affine scenes (world map) are a separate and probably easier case — the affine transform already scales natively, so a zoom there is close to free if the map buffer holds the data. Sprites are fine either way: the OBJ path already decodes the full 9-bit signed X (`gba_ppu.cpp:1259-1398`). Also check what a non-integer scale does to the existing integer-scaling and native-supersampling paths, which currently assume the stock pixel lattice. |
| WIDE-01 | Widescreen feasibility — studied 2026-08-14, verdict: mostly NO, one contingent maybe | The clamp is an active opt-out, not an oversight: `src/runner_main.cpp:2094,2098` hard-set `max_view_width`/`max_resize_view_width` to 240 and `:2102-2103` set `launcher_expose_widescreen=false` against an upstream default of `true`. Enforced generically in `gbarecomp/src/runtime/view_config.h:26-42`; engine ceiling is `kMaxRenderWidth=480` (`gba_ppu.h:121`). **A real wide-scanline renderer already exists and is per-scanline-correct** — `render_scanline_wide` (`gba_ppu.cpp:924-1440`) runs at true HBlank with that scanline's live IO and captured affine reference, and the OBJ loop decodes the full 9-bit signed OAM X, so off-window sprites already render correctly. **But it was built and proven for The Minish Cap, not Golden Sun**: the only off-screen tile provider, `gbarecomp/src/debug/ws_sidecar.cpp`, is hardcoded to Minish symbols (`gBGTilemapBuffers1`, `gMapHeader`, `CB2_Overworld`, `DrawMetatileAt`) and nothing in `src/` or `config/usa/main.toml` references it. Per scene type: **regular tiled BG (towns/dungeons/field) = NOT FEASIBLE** — the tilemap is only a 256/512px ring and wraps immediately, so margins show repeats, and GS has no equivalent map-streaming hook. **WIN0/WIN1-heavy scenes (menus, dialogue) = correctly pillarboxed to black by design** (`black_nonuniform_window_margins`, `gba_ppu.cpp:1006-1008,1409-1416`) and should not be widened. **World map (affine mode 2) = the only plausible win**, since an affine layer is a flat `128<<size_code` px buffer that may already hold real world data beyond 240. Also noted: mosaic is unimplemented repo-wide (zero grep hits) — a pre-existing PPU gap, not widescreen-specific. | **One cheap measurement decides the world-map case:** read `BG2CNT`/`BG3CNT` size_code and wrap bit live on the world-map savestate (slot 6) via the existing TCP debug server. If the affine map holds real data past 240 and wrap is off, widescreen there is close to plug-and-play (flip the width opt-in plus a scene pillarbox policy). If not, it is dead for that scene too. Two further TODO-EVIDENCE items before any field-map attempt: whether GS keeps OAM entries for actors outside the 240 window, and whether GS's own camera clamps / UI layout assume 240. |
| UI-03 | **NEXT SESSION (user's pick for tonight).** F1 config menu keeps resizing and responds badly | User report 2026-08-14 on the current build: "F1 menu is very annoying, keep resizing and not responding properly." Almost certainly a regression from UI-02/UI-02b, which rewrote `draw_hotkeys_tab` in `gbarecomp/src/runtime/host_config_ui.cpp` into a 5-column table (Action \| Keyboard \| clear \| Controller \| clear) and added a "Turbo Toggle" row. A tab wider than the others makes an auto-sized ImGui window jump size when switching tabs — matches "keeps resizing". **Treat "not responding" as a possibly SEPARATE cause**: UI-02b added `SDL_CONTROLLERAXISMOTION` edge synthesis in `pump()` (`host_window.cpp`, `kTriggerPressThreshold`=20000 / `kTriggerReleaseThreshold`=12000), and analog triggers emit motion continuously; a trigger resting near a threshold, or a controller with drift, would spam events and could flood `config_ui_capture_pad`. A worker was started on this and stopped before making any edit — the tree is clean, nothing half-applied. | Investigate both halves separately. Fix window sizing so it does not jump between tabs (prefer pinning a size or sizing to the widest tab once, over restructuring). Keep the controller column and Turbo Toggle row — both are features the user asked for. Do NOT change binding semantics, the Held/Toggle rule, or the persisted config format; those are user-confirmed working. Also queued and not started: 3-frame and 4-frame temporal blend levels (see VFX-FLICKER-01), and conservative visual polish of the menu — both touch the same files, so sequence them after the fix. |
| BIN-01 | **NEXT SESSION (user's pick for tonight).** Binary is very large — 881 MB | `build/gs011_opt/GoldenSunRecomp.exe` is 881,861,146 bytes at RelWithDebInfo (`-O2 -g -DNDEBUG`); the old Debug `build/gs011` was 486,926,134. The size is dominated by debug info, which optimization inflates further through inlining. | **Key fact that makes this cheap: the crash-ring symbol names this project actually relies on come from the generated `local/gs011/main/symbol_map.cpp`, NOT from DWARF debug info** — CRASH-01 was diagnosed entirely from those. So stripping or splitting DWARF likely costs very little real debuggability. Options in rough order of effort: build `Release` (`-O3 -DNDEBUG`, no `-g`); keep `-g` but `strip` the shipped copy; or `-gsplit-dwarf` to keep symbols in a side file. Measure size AND speed for whichever is chosen — `-O3` vs `-O2` may also move the perf number, so re-run the PERF-06 benchmark (600 headless frames, warmup discarded, 5 runs, idle machine) rather than assuming. Do NOT enable LTO: two prior attempts produced 0-byte binaries and drove this 16.7GB machine to ~1.2GB free. |
| PERF-06 | **`build/gs011` was an unoptimized Debug build — fixed by building `build/gs011_opt`.** | Found 2026-08-14 while explaining an exe size jump (231,882,858 → 486,926,134 bytes after a reconfigure forced a full recompile; earlier incremental builds mixed stale objects and hid it). `build/gs011/CMakeCache.txt` had `CMAKE_BUILD_TYPE=Debug` with `CMAKE_CXX_FLAGS_DEBUG=-g` and **no `-O` flag anywhere** — grepping its `build.ninja` for `-O[0-9s]` returned zero matches against 685 `-g`. Both presets in the top-level `CMakePresets.json` (lines 15, 29) also specify Debug. Built `build/gs011_opt` side-by-side at RelWithDebInfo (`-O2 -g -DNDEBUG`, LTO left OFF — two prior attempts produced 0-byte binaries and drove this 16.7GB machine to ~1.2GB free); `build.ninja` there confirms 685 `-O2`. `build/gs011` was deliberately NOT overwritten, since it is the binary every documented measurement is keyed to. All three C++ test targets pass at `-O2`: codegen_tests 131/131, mp2k_shadow_tests, temporal_blend_tests 3/3. **Measured ~2.9x faster**, 600 headless frames, warmup discarded, 5 runs each, idle machine: Debug 6698/6793/6883/6908/6993 ms vs Opt 2333/2348/2378/2381/2393 ms. (An earlier 3.7x figure was contended by a stray benchmark script and is superseded.) `src/launcher_main.cpp:363-371` now prefers `gs011_opt` and falls back to `gs011`. | **Bears directly on PERF-05** — a prologue scene that stayed slow even at 4x guest CPU overclock, which already proved the bottleneck was not guest CPU but something host-side. Re-test PERF-05 on the optimized build before investigating it further. Also casts doubt on the "167ns per guest instruction, interpreter-grade" figure in `docs/CODEX_HANDOFF.md` section 3 — that measurement should be repeated on `gs011_opt` before any conclusion is drawn from it. Still unresolved: whether Debug was deliberate for `gs011`, and whether the presets should change. |
| PERF-04 | **RESOLVED 2026-08-14 by PERF-06 (the optimized build); user-confirmed "name entry screen no longer slow".** Name-entry screen lags on a fresh boot (no savestate) | Reported by the user 2026-08-14. Booting the game normally and reaching the character name-entry screen stutters. User's read, unconfirmed: it looks like compile stutter, i.e. the same first-touch overlay/RAM-code compile cost as PERF-01 rather than steady-state CPU load. Not yet measured. Note this is a *fresh boot* path, so the warm cache may be cold for these routines — that is the obvious first thing to check. | User is playing through the whole intro to see how widespread it is. Wait for that report before instrumenting. Then: capture a fresh-boot session log and check interpreted_insns / ram_crc_mismatches / worst-frame around the name-entry screen, same metrics as the PERF-01 before/after table. |
| PERF-05 | Prologue boulder scene not full speed, even at 4x CPU overclock | Reported by the user 2026-08-14 during a full intro playthrough. **The 4x result is the important part.** The overclock only scales guest CPU work; it deliberately does not scale halt/idle, DMA-stolen cycles, or IRQ wake latency, and it does not touch PPU render or host present cost at all. So a scene that stays slow at 4x is evidence that the bottleneck is *not* guest CPU work — which rules out the PERF-01 "real GBA is legitimately CPU-bound" explanation for this scene. Remaining candidates: our own compile storms (first-touch overlay/RAM code), PPU render cost, or a host-side present/pacing stall. Not yet measured. | Ask the user for a savestate taken just before the boulder scene — unlike the barrier, this one is a sustained slow section, so it should be reachable and measurable. Then run the scene at 1x and 4x and compare frame times: if the two are the same, guest CPU is confirmed not the bottleneck and the cost is ours. Check interpreted_insns and worst-frame the same way as PERF-01. |
| UI-02 | Controller support for hotkeys; Turbo Toggle vs Turbo Held | Feature request from the user 2026-08-14, not a bug. Three parts: (1) hotkeys should be bindable to controller buttons, not keyboard only; (2) add a **Turbo Toggle** hotkey — press once to turn turbo on, press again to turn it off; (3) the existing turbo hotkey becomes **Turbo Held** — active only while the button is down. Both modes should coexist and be separately bindable. Relates to UI-01 (turbo audio, ceiling, and the `[turbo-present]` skip accounting) — reuse that turbo path rather than adding a second one. | Locate the existing hotkey binding and turbo activation path in the host UI/input layer, confirm whether any controller input is already plumbed through, then spec the binding UI before implementing. Persist both bindings alongside the other config settings. |
| VFX-LINGER-01 | Sprites linger over the black screen during the battle-exit transition | Reported by the user 2026-08-14. Small visual error, not a lag issue. On the fade-out at the end of a battle, the screen goes black but sprites remain visible over it instead of fading with everything else. Untested hypothesis: the fade is a brightness/blend effect (BLDY / BLDCNT effect 2-3) applied to the background layers but not to the OBJ layer — i.e. OBJ is missing from the blend target set, or our PPU applies the brightness effect to BG only. That is a concrete, checkable claim about our renderer, not about the game. | Reachable and repeatable — happens at the end of any battle, so it needs no special savestate. Read BLDCNT/BLDY and the per-layer blend targets across the fade frames using the existing TCP debug server (`ppu_state`), and compare which layers our PPU actually applies the brightness effect to. Do not change rendering code until the register evidence says which layer is being skipped. |
| AUD-01 | Possible small sound glitches on item use / shop buy-sell | Reported by the user 2026-08-14, flagged by them as **unsure whether it is a real bug** — they could not tell if what they heard differs from the original game. Suspicion only, same footing as PERF-03. Menu item use and shop buy/sell are short one-shot sound effects fired while the sound driver is already running, so the plausible failure modes are a dropped or truncated one-shot, a wrong channel steal, or a mixing artifact — none confirmed. **Added 2026-08-14, same session:** the user also reports some sounds seem **too loud**, and names these same store/menu sounds as the likely example. Loudness is a more tractable symptom than "glitch" — it points at per-channel volume/mixing scaling rather than a dropped one-shot, and it is measurable without needing to judge whether a sound is subjectively wrong: peak amplitude of a given one-shot can be compared against a reference directly. **AUD-01a review done 2026-08-14 — my "wrong volume ratio" hypothesis is REFUTED, but a real bug was found.** The whole mix path was cross-diffed line by line against the vendored mGBA reference (`gbarecomp/third_party/mgba/src/gba/audio.c`, `.../gb/audio.c`): SOUNDCNT_H PSG ratio and DirectSound full-volume bits, SOUNDCNT_L master nibbles and routing, SOUNDCNT_X, and the SOUNDBIAS/×48/clip stage are all bit-exact matches. The final stage saturates, it does not wrap. No unrequested gain exists anywhere between MP2K's output and the host device — the MP2K shadow/native mixer is never armed in the launcher config (`audio_shadow` defaults false, no `[audio].shadow` key, launcher sets only `AUDIO_NATIVE=0`/`AUDIO_STEREO=1`), and the host volume knob defaults to a no-op 100%. Stereo applies no extra gain over mono; they are two correct representations, not one scaled twice. **The real defect found: `gba_audio.cpp:1116-1117` adds ch3 (SOUND3, wave RAM) without the `<< 3` the other three PSG channels get, so SOUND3 is ~8× TOO QUIET.** That is the opposite polarity of the user's report — but a channel 8× too quiet makes everything else sound too loud by comparison, so it plausibly IS this symptom reported from the other side. Fix in progress as AUD-02. | Still open after AUD-02 lands: |
| Do NOT instrument yet; there is no confirmed defect to chase, and the user's own uncertainty is the blocker. Needs a specific reproducible action first ("selling any item in the Vale shop clicks"). Once named: compare against a known-good reference of the original at that same action. Consider folding into PERF-03, which is the same class of unconfirmed audio suspicion. |
| VFX-FLICKER-01 | Kolima blue barrier renders opaque and on only one character | Confirmed repro (not a suspicion), savestate slot 2 (user loads via F3 / state 2). Expected, per the user (authoritative on original-game behavior): the blue barrier is TRANSLUCENT and appears on ALL THREE party members simultaneously — accurate original-hardware behavior. Actual: renders fully OPAQUE, on only ONE character. Leading hypothesis, UNPROVEN: the game may draw the barrier for one character per frame, cycling across the three; a real GBA LCD's slow pixel response would smear that into three simultaneous translucent barriers, while this port's instant-pixel rendering shows one opaque one instead. Not yet ruled out: real BLDALPHA alpha blending rendered incorrectly by our PPU, or a sprite-count/priority limit dropping sprites on some frames. Under investigation by another worker. **UPDATE 2026-08-14, user play-test: the per-frame-cycling hypothesis is effectively CONFIRMED.** The user reports the barrier is now BETTER but "still shimmers", and that the world map shimmers the same way — reframing the symptom from "opaque on one character" to flicker. That matches alternating-frame drawing exactly: the game fakes translucency by drawing on alternating frames, a real GBA's slow LCD smears them into a steady translucent image, and our instant-pixel display shows the alternation as shimmer. Corroborating evidence already on file: the world-map artifact was independently measured alternating on an EXACT 2-frame period, and our PPU has no sprite-drop budget, so "one barrier drawn" means the game submitted one that frame. | Fix in progress as VFX-FLICKER-02: an opt-in whole-frame temporal blend ("LCD ghosting"), default OFF, with named strength levels — the strongest being a true 50/50 two-frame average, which turns an exact 2-frame alternation into a steady image. Note this is DIFFERENT from the existing spatial `Reduce shimmer (soft filtering)` option, which blurs the picture without stopping flicker. The user explicitly wants the world map stable even though its shimmer is GBA-accurate, so deviating from frame-exact output is desired here — but only opt-in, never default. |

Do not conflate PERF-01 with PRES-01: the latest Enhanced-only battle interval
was the worst measured compiler backlog, while the later no-enhancement field
interval was nearly clean. The user's smoother observation was real cadence
improvement plus different scene content.
