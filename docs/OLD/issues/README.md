# Archived issue records

Superseded on 2026-09-04 when the project's goal, direction and scope were
restated. See `../DIRECTION_2026-09-04.md` for what is current.

These files remain accurate as records of what was true when they were written,
and several contain measurements and dead ends worth not repeating. They are
archived because their *direction* is superseded, not because their facts are
wrong.

Notable: `NATIVE-CODE-DIRECTION.md` (survey of what is recompiled vs native,
and the function/code-size census), the `SESSION_HANDOFF_*` series, and the
`WIDE-01*` widescreen investigation records.
