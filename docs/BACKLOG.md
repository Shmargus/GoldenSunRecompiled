# Backlog

Wishlist and future enhancements. These are not current work; do not load this
file for an unrelated task.

- Mod hook policy.
- Custom items/weapons: first measure item and unleash tables and ID width.
- Custom Psynergy: first locate its table and ID width.
- Additional ROM regions.
- Higher-quality native audio after canonical MP2K replacement is proven.
- Widescreen product polish after manual multi-scene acceptance.
- Camera-zoom-out control (former WIDE-02): first prove resident map data,
  affine coverage, sprite/UI behavior, and scaling-lattice safety.
- Faster or instant text: first locate the live setting read path in the
  windowed build; do not reuse the old unverified RAM candidates.

Baseline work always takes priority. A wishlist item becomes current work only
when it receives an issue/feature file and an entry in `NEXT_TASK.md`.
